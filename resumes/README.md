# resumes/

Drop your resume here (`.pdf`, `.docx`, `.txt`, `.md`).

Everything in this folder except this README is gitignored, so nothing
personal is committed. The repository is public — keep it that way.
